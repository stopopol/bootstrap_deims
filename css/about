/* =========================================================
   DEIMS About Page (/about) — white card container
   ========================================================= */

/* White rounded container wrapping the h1 + content */
body.path-about .main-container .row {
  background: var(--landing-page-bg);
  border: 0.5px solid var(--landing-page-border);
  border-radius: var(--landing-page-radius-lg);
  padding: 1.75rem 2rem;
  max-width: 1300px !important;
}
 
